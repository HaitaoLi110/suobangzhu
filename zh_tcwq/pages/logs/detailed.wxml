<view class="container" wx:if="{{state==1}}" wx:for="{{detaileds}}" wx:key="item">
    <view class="detailed">
        <view class="view_zt">等级：{{item.title}}</view>
        <view class="view_zt">支付状态：{{item.status }}</view>
    </view>
    <view class="detailed_info"><text>价格 ：{{item.price}} 元</text>   <text>时间：{{item.addtime}}</text></view>
</view>

