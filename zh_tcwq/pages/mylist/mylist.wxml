<view class='content'>
	<view class='info' wx:for="{{list}}" wx:key="{{item}}">
		<image src="{{item.url}}" class='infoImg'></image>
		<view>{{item.name}}</view>
		<view>{{item.tel}}</view>
		<view>{{item.chanpin}}</view>
		<view>{{item.address}}</view>
	</view>
	
</view>